class name : public terr {
  public:
    name`()': terr(
              food, // Food
              production, // Production
              trade, // Trade
              ifdef(water_tile, water_tile, false) // Is water?
              ) {}
};
